# React Redux Starter

## Build Setup

``` bash
# install dependencies
$ npm install

# serve with hot reload at localhost:8100
$ npm run dev

# build for production and launch server
$ npm run prod
$ npm run start

```
